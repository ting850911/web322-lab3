doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Climate Solutions
    link(rel="stylesheet" href=`/css/main.css?ts=${timestamp}`)
    link(rel="stylesheet" href=`/css/custom.css?ts=${timestamp}`)
  body
    include partials/navbar.pug
    .min-h-screen.container.mx-auto
      .hero.min-h-screen.bg-base-200.main-view
        .hero-content.text-center
          .max-w-md
            h1.text-5xl.font-bold Explore Projects
            p.py-6 Discover innovative solutions and projects aimed at tackling climate change.
            span.material-symbols-outlined
      div(class="grid grid-cols-1 md:grid-cols-3 gap-4 p-4")
        each project in projects
          .card 
            img(src=project.feature_img_url alt=`Project ${project.id} Image`).card-img-top
            div(class="card-body relative py-2 px-1 h-2/5")
              h5.card-title= project.title
              p.card-text= project.summary_short
              a(href=`/solutions/projects/${project.id}` class="absolute right-0 bottom-0 text-info") Learn More
    include partials/footer.pug